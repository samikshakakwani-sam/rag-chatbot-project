# Phase 1 — Data Ingestion & Parser
# (uses only Python stdlib: html.parser, json, re, pathlib, urllib)

# Phase 3+ — Backend Server
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
httpx==0.27.2

# Phase 2 — Scheduler
apscheduler==3.10.4

# Phase 2.7 — Vector Embedding & Store
# BGE-small (BAAI/bge-small-en-v1.5) embeddings, local & free; persistent vector DB.
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Phase 4 — LLM Client (Groq, OpenAI-compatible chat API)
groq>=0.11.0

# Phase 7 — Testing
pytest>=8.0.0

# Streamlit Community Cloud entry point (streamlit_app.py)
streamlit>=1.39.0
