# Streamlit Community Cloud deploy — LEAN, keyword-only retrieval.
# No PyTorch / ChromaDB / sentence-transformers → fits the free-tier RAM.
# (The full FastAPI/Docker stack with semantic search is in requirements-docker.txt.)
streamlit>=1.39.0
groq>=0.11.0
python-dotenv>=1.0.1
pydantic>=2.0
